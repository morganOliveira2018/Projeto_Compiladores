
Inicio

  inteiro a = 3
  real b = 10.5, c = 30.5, area = a * b * c
  leitura(a)
  texto autor = "morgana"
  escreva(a) @@ escreva o valor da variavel a
  escreva(b) @@ escreva o valor da variavel b
  escreva(c) @@ escreva o valor da variavel c
  escreva(area) @@ 

  IF(a > b){ 
    while(a*a>b){  
      a-- 
    }
  }ELSE{
    escreva(b)
  }

  inteiro x 
  FOR x = 1; x < 10; x = x + 1 { 
    x = x + 2 
  } 
  escreva(x)

  @@ operador ternario
  inteiro num = -5 
  num < 0 ? escreva("sim") : escreva("nao") ; 
  
  escreva("autor")
  escreva(autor)

Fim do programa